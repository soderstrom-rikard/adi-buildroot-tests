#!/usr/bin/expect

cd $src_root
set timeout 60
spawn make $board_defconfig

while 1 {
    expect {
        "configuration written to" {}
        eof { break }
        timeout {
            puts "\n\nERROR: make default config timeout"
            break
        }
    }
}
