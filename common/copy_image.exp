#!/usr/bin/expect

set timeout 8
cd $src_root
catch {exec cp output/images/$boot_image /tftpboot}

if { $HAVE_KERNEL_ONLY_IMAGE == "1" } {
    catch {exec cp output/images/$compressed_kernel_only_image /tftpboot}
}

send_log "Copy Images Finished!\n"
